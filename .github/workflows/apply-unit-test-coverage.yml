name: Apply Unit Test Coverage

on:
  pull_request:
    branches: [master]

permissions:
  contents: write

concurrency:
  group: apply-unit-test-coverage-${{ github.event.pull_request.number }}
  cancel-in-progress: false

jobs:
  apply-test-commit:
    if: github.event.pull_request.head.ref == 'agent/unit-test-coverage'
    runs-on: ubuntu-latest
    steps:
      - name: Check out implementation branch
        uses: actions/checkout@v4
        with:
          ref: agent/unit-test-coverage
          fetch-depth: 0

      - name: Decode and apply implementation patch
        run: |
          cat tools/unit-test-coverage.parts/* > "$RUNNER_TEMP/unit-test-coverage.patch.gz.b64"
          test "$(wc -c < "$RUNNER_TEMP/unit-test-coverage.patch.gz.b64")" -eq 24084
          base64 --decode "$RUNNER_TEMP/unit-test-coverage.patch.gz.b64" > "$RUNNER_TEMP/unit-test-coverage.patch.gz"
          gzip -t "$RUNNER_TEMP/unit-test-coverage.patch.gz"
          gzip -dc "$RUNNER_TEMP/unit-test-coverage.patch.gz" > "$RUNNER_TEMP/unit-test-coverage.patch"
          git apply --check "$RUNNER_TEMP/unit-test-coverage.patch"
          git apply "$RUNNER_TEMP/unit-test-coverage.patch"

      - name: Format Rust code
        run: cargo fmt --all

      - name: Check formatting
        run: cargo fmt --check

      - name: Run clippy
        run: cargo clippy --workspace --all-targets --all-features -- -D warnings

      - name: Run Rust tests
        run: cargo test --workspace --all-features

      - name: Run dependency and license checks
        uses: EmbarkStudios/cargo-deny-action@v2

      - name: Commit validated implementation
        run: |
          rm -rf tools/unit-test-coverage.parts
          rm -f tools/unit-test-coverage.patch.gz.b64
          rm -f tools/unit-test-coverage-followup.patch
          rm -f .github/workflows/apply-unit-test-coverage.yml
          rm -f .github/workflows/patch-diagnostic.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "Implement unit test coverage TODO"
          git push origin HEAD:agent/unit-test-coverage

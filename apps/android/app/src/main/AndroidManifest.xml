<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
    allowBackup is deliberately false: Android's built-in cloud/adb backup would copy the
    canonical SQLite library outside this app's own encrypted .atnb backup path, undermining
    spec section 22.3's "no fallback from requested encryption to plaintext" guarantee.
    -->
    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/full_backup_content"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.A2dNotebook">

        <activity
            android:name=".app.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
